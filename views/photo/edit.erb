<h1>Edit this Photograph</h1>

<form action="/photos/<%= @photo.id %>" method="post">
    <input type="hidden" name="_method" value="put"><br>

    <label for="name">Photograph Name:</label>
    <input type="text" name="name" value="<%= @photo.name %>">

    <label for="object">Link to Photograph:</label>
    <input type="text" name="object" value="<%= @photo.object %>">

    <label for="datetaken">Date Taken:</label>
    <input type="date" name="datetaken" value="<%= @photo.datetaken %>">

    <label for="location">Location:</label>
    <input type="text" name="location" value="<%= @photo.location %>">

    <label for="aperture">Aperture Used (f):</label>
    <input type="number" name="aperture" step="0.1" value="<%= @photo.aperture %>">

    <label for="shutterspeed">Shutter Speed Used (e.g. 1/125 s):</label>
    <input type="text" name="shutterspeed" value="<%= @photo.shutterspeed %>">

<!-- following 3 selects dynamically create pulldown options and prefills with appropriate info -->
    <label for="photographer_id">Select your Username:</label>
    <select name="photographer_id">

        <% @photographers.each do |photographer| %>
        <option value='<%= photographer.id %>' <%= 'selected' if photographer.id == @photo.photographer_id %>><%= photographer.username %></option>
        <% end %>

    </select>

    <label for="camera_id">Select the Camera used:</label>
    <select name="camera_id">

        <% @cameras.each do |camera| %>
        <option value='<%= camera.id %>' <%= 'selected' if camera.id == @photo.camera_id %>><%= camera.make %> : <%= camera.model %></option>
        <% end %>

    </select>

    <label for="lens_id">Select the Lens used:</label>
    <select name="lens_id">

        <% @lenses.each do |lens| %>
        <option value='<%= lens.id %>' <%= 'selected' if lens.id == @photo.lens_id %>><%= lens.make %> : <%= lens.model %></option>
        <% end %>

    </select>

  <input type="submit" value="Edit your photograph">

</form>

<form action="/photos/<%= @photo.id %>" method="post">
  <input type="hidden" name="_method" value="delete">
  <input type="submit" value="Delete photograph">
</form>

<br>
<br>
<a href="/photos">Back</a>
